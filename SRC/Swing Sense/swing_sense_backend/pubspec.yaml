name: swing_sense_backend
description: API do Swing Sense - rede social de treinos de tenis integrada a raquete inteligente (ESP32).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  dart_jsonwebtoken: ^2.13.0
  bcrypt: ^1.1.3
  uuid: ^4.4.0
  postgres: ^3.5.12

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.7
